# 随行身份与对话旁白调整（2026-09-11）

## 日志结论

用户报告的詹姆斯随行与冗长问答分别对应 `c628466b…` 和 `c70efa44…`。

- 引擎没有解除随行。公开事件记录了 `accompanying=true`，以及随队进入度假村边界、城郊道路、蛙鸣泉的 `entity.moved(reason=accompanying)`；最终状态仍为随行。
- 进入蛙鸣泉的叙事却将詹姆斯写成坐在池边、初次遇见的人，还生成了重新询问来意的台词。错误发生在叙事表达，而非随行绑定或位置提交。
- “你为什么不愿意回家”已经产生了詹姆斯的独立回答；守秘人正文仍先用两段神态描写延长迟疑。这不是缺少 NPC 台词，也不是兜底文案。
- 原提示在没有新结果时要求写神情动作，又将台词分离后的旁白限定为动作神态；重试示例使用“沉默片刻才开口”。这些要求与普通问答应简短、旁白应承接实际台词的目标冲突。

## 补充核查：换场景时的 NPC 记忆

对“去蛙鸣泉”回合，以 2026-09-11 14:48:50（北京时间）前的数据库副本和最终公开视图 revision 39，调用现有 `_read_memory_context`、`SqlAlchemyMemoryStore.read_npc_context`，并用本地捕获 client 验证 `PromptActionPlanNarrationModel` 的 JSON 序列化。没有调用外部模型，也没有写入原游戏库。原日志没有保存完整模型请求体，因此这是历史状态与生产查询的本地重放，不是原始网络请求抓包；相关检索代码未被本次叙事提示调整修改。

检索对象包含当前可见的 `dream_frogs` 和 `james`。结果恰好两条，共 94 字符，未触及默认 12 条 / 4000 字符上限，两条均完整进入模型输入的 `memories`：

- 詹姆斯的上一轮回答：“……回去？我回不去了。你带我出来的时候，没看见那些东西吗？它们就站在池塘边上，笑都不笑地看着我。”
- 詹姆斯亲自听到玩家问“你为什么不愿意回家”的经历记录。

这两条的来源地点都是 `outside`，通过 `subject_id=james` 跨场景入选。回答在 14:48:45 已完成记忆投影，早于本次叙事生成；日志未见记忆读取降级。最终公开视图也已提供詹姆斯的 `observable_state.accompanying=true`。所以这次输入已有相识和随行依据，仍生成初遇是叙事未遵循已有上下文；新增随行名单是在突出已有状态，并非首次接入 NPC 记忆。

同时确认两处经历保留缺口：

- “你带着詹姆斯……走出度假村”的旁白虽然保存为记忆，参与者索引只有玩家和 `actor_1`，没有 `james`；查询新地点时被“同地点或相关实体”条件排除。`previous_published_narration` 只补充紧邻的一段旁白，此时已是问答前的神态描写，不包含更早的离开过程。该玩家没有对话摘要。
- `entity.state_changed(accompanying=true)` 和 `entity.moved(reason=accompanying)` 的事件 payload 没有展示文本，现有投影退化成 `adjudication:...` 原因串；参与者也只记 `actor_1`，未关联被移动的 NPC。这些记录又被检索中的原因串过滤条件排除，因此没有形成“詹姆斯与玩家一同移动”的可读经历。

后续记忆修复应在现有事件投影中表达并关联 NPC 的权威经历，保持受众权限；当前是否仍在随行继续以世界状态为准，不能仅凭历史对话反推。此次补充只核查与记录上述缺口，尚未修改记忆投影。

本地重放脚本与结果：`/tmp/trpg-companion-memory-20260911/inspect_memory.py`、`retrieval-results.json`。

## 本次修改

`ActionPlanNarrationContext.to_prompt_dict()` 从最终公开视图中提取 `accompanying_npcs`（仅 ID 和名称）：只包含可见 NPC 且 `observable_state.accompanying` 为布尔 true 的人物。既有场景实体与全部公开状态保留；持久化上下文、输出协议、规则引擎都不改变。旧请求、历史叙事和模组初始位置不能产生这份名单。

通用叙事提示替换冲突段落：当前随行身份优先于静态场景素材；区分刚加入和持续随行；旁白与同次输出的 NPC 台词一致。普通问答以台词为主，旁白通常一句，选择必要的动作或语气即可；正常回答不附带固定的沉默、迟疑前奏。NPC 气泡只承载台词，舞台动作由旁白负责。

仍保留首次抵达与新增事实的完整表达要求，不按固定字数截断信息。没有增加第二条模型链路、语义判定器或场景专用词表。原有台词分离重试提示也同步改为简短衔接实际回答。

## 验证

- 框架叙事回归：26 项通过，包括随行投影只依据当前公开状态、失去可见性或解除随行后不再进入名单、场景与持久化上下文不变。
- 后端叙事恢复与模型适配器回归：111 项通过。
- 后端 Ruff、格式、ty 和框架 Ruff、格式检查通过。
- 真实模型：DeepSeek `deepseek-chat`，两个完全虚构的最小场景，每个调用一次，均通过现有叙事校验。没有读取模组文件、存档、日志或玩家记录构造模型输入。

首次拟使用本地模组构造的真实测试被自动审批拒绝，未执行；随后先本地展示并核对纯虚构输入，再获准执行以下测试。因此这些是通用表达抽测，不是原始房间上下文的精确重放。

| 场景 | 旁白长度（含标点） | 人工核对 |
| --- | --- | --- |
| 向随行向导询问接下来往哪走 | 19 | 一句指路动作加独立回答，无沉默或长段迟疑 |
| 与向导进入旧车站并读到路线信息 | 93 | 二人一起抵达，保留北门目的地及小径封闭条件，无初次相遇描写 |

问答旁白：

> 林舟抬手指了指脚下这条大路延伸的方向。

向导回答：

> 顺着这条大路往北走就是北门，从北门能进村。林间那条小径前阵子落了石，已经封了，走不得。

抵达旁白：

> 周越和林舟走进旧车站的候车厅。下午的光从破窗斜进来，厅里摆着几排长椅，墙边钉着那张路线图——图注写得很清楚：北门通向村庄，林间小径因落石封闭，不能通行。林舟走到图前，把这两条又看了一遍。

少量抽测未复现随行身份重置或回答与旁白冲突；仍出现了输入中未指定的“破窗”等装饰性细节，以及路线信息在台词中的重复。没有据此增加场景专用修补或追加大量模型调用。表达质量仍由上下文和提示引导，本次不声称获得通用语义准确性保证。
