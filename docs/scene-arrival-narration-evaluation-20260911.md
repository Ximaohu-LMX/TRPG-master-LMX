**场景抵达叙事评估（2026-09-11）**

原问题：进入《幸福蛙蛙村》接待大厅时自动揭示地图信息，两次叙事均被 `required_evidence_missing` 拒绝，最后返回“已经抵达＋周围可见＋可见出口＋地图原文”的拼接回复。新增信息原文和地点全称的匹配要求都会拒绝语义合理的改述。

本次沿用现有叙事调用：以公开场景描述和新增信息为底稿，允许自然改写；来源声明须引用已提交结果。原文仍可用于自动恢复引用和确定性兜底。模型自报引用不等于已证明语义完整，不增加额外语义校验模型。

上下文去掉裁决选项、重复背景和重复信息正文，保留完整场景、人物身体条件、物品、实际结果、时序与历史。删句后清除信息及位置来源声明再复校验，避免删掉事实仍沿用原声明。兜底分段组织现场，保留公开事实原文。

使用 `deepseek-chat` 和正式模组 `happy-frog-village@3.0.14`，通过真实引擎在内存中依次进入大厅、二楼走廊、返回大厅，走生产 `_narrate` 的校验和最多一次重试；未修改真实房间。两轮各 3 个样本：首轮发现地点全称的同类误拒绝后做了一次代码调整，随后复测同组，不继续围绕偶发幻觉调参。

| 最终样本 | 模型调用次数 | 最终路径 | 同一最终输出交给旧校验 | 人工核对 |
| --- | --- | --- | --- | --- |
| 进入大厅 | 2 | `model` | `required_arrival_missing` | 地图的一楼布局、八间编号客房、杂物间及进入限制均有表达；首轮因 NPC 引语混入正文重试。艾米丽和詹姆斯出现，幸福信使漏写。 |
| 前往二楼客房走廊 | 1 | `model` | `required_arrival_missing` | 自然描述编号房门、杂物间和连接楼下的路径；门缝等细节有模型补写。 |
| 返回大厅 | 1 | `model` | `accepted` | 自然描写返回后的现场；没有重复全文地图资料，篇幅仍偏长。 |

最终 3 组均返回模型叙事，0 组使用兜底；这只是抽样观察，不是成功率或完整性保证。角色覆盖、重访篇幅及来源外的装饰细节仍有偏差，按本轮要求记录，不为单次样本添加场景规则。

大厅最终样本中的地图段落：

> 左手边是一组沙发和一张矮茶几，茶几上的玻璃盘里摆着几颗用锡纸包好的糖果。正对面的墙上挂着一幅装裱好的楼层地图，你的目光扫过去，把上面的房间分布看了个清楚：一楼这侧是接待处、餐厅兼酒吧、后厨，老板宿舍和员工宿舍各自用一道门隔开；靠右的楼梯通往二楼，走廊两侧排着从1号到8号的客房，尽头还有一间杂物间。地图上餐厅、员工宿舍、老板宿舍和二楼杂物间的位置，都标着颜色不同的门框线——只是标在那里而已，看不出哪些是你能推门进去的。

自动验证覆盖自然改述及地点称呼、引用范围、原文样式豁免、删句后保留信息、完整场景上下文、抵达与失败/澄清组合，以及现有协议和隐藏内容边界。后端全部 765 项用例分批覆盖：742 passed、23 skipped；Framework 定向回归 135 passed、架构检查 14 passed；Ruff、格式和 ty 检查通过。真实模型最终 3 组均返回模型叙事，0 组使用兜底（调用次数 2 / 1 / 1）。 后端测试进程使用不含开发 `.env` 的工作目录及独立临时数据库；相对图片资源路径映射回仓库。
